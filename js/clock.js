/* ===== CLOCK WIDGET ===== */

.clock {
  display: flex;
  align-items: center;
  justify-content: center;
}

.clockBig {
  font-size: clamp(72px, 10vw, 120px);
  font-weight: 900;
  letter-spacing: -2px;
  line-height: 1;
  text-align: center;
}
